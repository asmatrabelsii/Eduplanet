<div class="d-flex flex-row" style="margin-right: 95px; margin-bottom:20px">
  <div class="flex-column">
    <h4 class="ms-4 mt-3">Filtrer:</h4>
  </div>
  <div class="flex-column mt-3 ms-5">
    <div class="dropdown">
      <a class="btn btn-secondary dropdown-toggle" href="#" role="button" id="dropdownMenuLink" data-bs-toggle="dropdown" aria-expanded="false">Catégories</a>

      <ul class="dropdown-menu" aria-labelledby="dropdownMenuLink">
        {% for option in options %}
          <li>
            <a class="dropdown-item" href="{{ path('cathegorie_index', { libelle: option.libelle }) }}">{{ option.libelle|capitalize }}</a>
          </li>
        {% endfor %}
      </ul>
    </div>
  </div>
</div>
