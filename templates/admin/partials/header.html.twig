<nav class="navbar navbar-expand-lg p-3 shadow-sm nav-color">
	<a href="{{ path('homepage') }}"><img src="/logo.png" class="logo"></a>
	<button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
		<span class="navbar-toggler-icon"></span>
	</button>
	<div class="container-fluid justify-content-end align-items-center collapse navbar-collapse" id="navbarNav">
		<ul class="navbar-nav">
			<li class="nav-item">
				<a class="item-link text-decoration-none text-dark" href="{{ path('admin_dashboard') }}"><i class="fa-solid fa-chart-line"></i> Dashboard</a>
			</li>
			<li class="nav-item">
				<a class="item-link text-decoration-none text-dark" href="{{ path('admin_cours_index') }}"><i class="fa-solid fa-book"></i> Cours</a>
			</li>
			<li class="nav-item">
                <a class="item-link text-decoration-none text-dark" href="{{ path('admin_cathegories_index') }}"><i class="fa-solid fa-list"></i> Catégories</a>
            </li>
            <li class="nav-item">  
                <a class="item-link text-decoration-none text-dark" href="{{ path('admin_comment_index') }}"><i class="fa-solid fa-comments"></i> Commentaires</a>
            </li>
			<li class="nav-item">
				<a class="item-link text-decoration-none text-dark" href="{{ path('admin_utilisateur_index') }}"><i class="fa-solid fa-users"></i> Utilisateurs</a>
			</li>
			<li class="nav-item">
				<a class="item-link text-decoration-none text-dark" href="{{ path('admin_certification_index') }}"><i class="fa-sharp fa-solid fa-graduation-cap"></i> Certifications</a>
			</li>
        </ul>
		<ul class="navbar-nav">
			<li class="nav-item dropdown">
				<a class="nav-link dropdown-toggle" id="paymentDropdownLink" href="#" data-toggle="dropdown">
					<i class="fa-sharp fa-solid fa-cart-shopping"></i> Paiements
				</a>
				<div class="dropdown-menu dropdown-menu-right" aria-labelledby="paymentDropdownLink">
					<a  class="dropdown-item" href="{{ path('admin_payment_index') }}">Paiements des cours</a>
					<a  class="dropdown-item" 	href="{{ path('admin_payment_examen_index') }}">Paiements des examens</a>
				</div>
			</li>
		</ul>
        <ul class="navbar-nav ml-auto">
			{% if app.user %}
                <li class="nav-item dropdown">
                    <a class="nav-link dropdown-toggle" id="compteDropdownLink" href="#" data-toggle="dropdown">
                        <img src="{{ asset('/uploads/avatars')}}/{{ app.user.avatar }}" alt="Avatar de {{ app.user.fullName }}" class="avatar avatar-mini">
                        {{ app.user.fullName }}
                    </a>
                    <div class="dropdown-menu dropdown-menu-right" aria-labelledby="compteDropdownLink">
                        <a class="dropdown-item" href="{{ path('admin_compte', {'id': app.user.id }) }}">Mon compte</a>
                        <a class="dropdown-item" href="{{ path('app_logout') }}">Se déconnecter</a>
                    </div>
                </li>
			{% endif %}
		</ul>
	</div>
</nav>

{% for label, messages in app.flashes %}
	<div class="container">
		<div class="alert alert-{{ label }}">
			{% for message in messages %}
				<p>{{ message | raw }}</p>
			{% endfor %}
		</div>
	</div>
{% endfor %}
