<html>

<head>
    <style type='text/css'>
        body {
            color: black;
            font-family: Calibri;
            font-size: 24px;
            background-image: url(data:image/png;base64,{{ border_base64 }}) ;
            background-repeat: no-repeat;
            background-size: 100% 100%;
        }

        .logo {
            width: 160px;
            height: 40px;
            margin: 50px 10px 10px 0;
        }

        .marquee {
            color: #000000;
            font-size: 45px;
            text-transform: uppercase;
            margin: 20px;
            font-weight: bold;
        }

        .assignment {
            margin: 20px;
            font-size: 22px;
        }

        .person {
            font-size: 28px;
            font-weight: bold;
            margin: 20px auto;
            width: 400px;
        }

        .course {
            font-size: 30px;
            margin: 20px auto;
            width: 400px;
        }

        .reason {
            margin: 20px;
            font-size: 22px;
        }

        .container{
            text-align: center;
            padding-top: 50px;
            margin-right: 30px;
        }
        .container2 {
            text-align: right;
        }

        .container2 img {
            margin-right: 12%;
            margin-bottom: 5%;
        }
    </style>
</head>

<body>
    <div class="container">
        <div>
            <img src="data:image/png;base64,{{ logo_base64 }}" class="logo" alt="Logo"/>
        </div>

        <div class="marquee">
            Certificate of Completion
        </div>

        <div class="assignment">
            This certifies that
        </div>

        <div class="person">
            {{ student_name }}
        </div>

        <div class="reason">
            Has complited the necessary courses of study and passed<br />
            the {{ exam_name.cours.titre }} exam and is hereby declared a
        </div>
        
        <div class="course">
            Certified in {{ exam_name.cours.titre }}
        </div>

        <div class="reason">
            Issued {{ date |date ('m/d/Y') }}
        </div>

    </div>

        <div class="container2">
            <img src="data:image/png;base64,{{ signature_base64 }}" alt="signature"/>
        </div>
</body>

</html>